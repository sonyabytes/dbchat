---
"@dbchat/web": patch
---

Clean up the update tooltip's release notes: strip the "What's Changed" / "Full Changelog" boilerplate, `by @user in <url>` attribution, bare URLs, and stray markdown so the notes read as plain, human-readable bullets.
