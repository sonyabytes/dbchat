---
"@dbchat/server": minor
"@dbchat/web": minor
---

Add functional Codex and OpenCode agent drivers with CLI runtime discovery, streaming responses, resumable provider sessions, and the same guarded database tools and write-approval flow used by Claude.

Codex connects through app-server dynamic tools, while OpenCode uses a turn-scoped authenticated MCP bridge. Provider models become selectable automatically when their CLI is installed and configured.
