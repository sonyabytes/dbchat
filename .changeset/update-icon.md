---
"@dbchat/web": minor
"@dbchat/desktop": minor
---

Replace the sidebar "Check for updates" button with a single status icon. Hover shows the version diff and release notes; click downloads the update, then the icon switches to "restart to install". Background update checks no longer pop a dialog — they just light up the icon.
