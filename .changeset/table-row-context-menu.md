---
"@dbchat/web": patch
---

Make table rows easier to select and move row actions to a right-click context menu. Clicking anywhere on a row now toggles its selection (shift-click selects a range) instead of only the row-number gutter. The table browser's selection action bar that popped up above the grid is replaced by a per-row context menu with copy as JSON / INSERT, CSV export, and "Ask about this table".
