---
"@dbchat/web": patch
---

Settings → Providers now keeps a default model per provider. Picking Codex no longer shows the Anthropic server default (Sonnet 5); each provider card lists only its own models, and a "Use for new chats" button chooses which provider new chats start on.
